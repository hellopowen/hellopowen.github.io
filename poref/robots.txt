User-agent: *
Allow: /

# Crawling is allowed on purpose. The page carries <meta name="robots" content="noindex">,
# and a crawler must be able to READ the page to obey it — a Disallow here would block the
# fetch and leave the URL indexable with no content. Nothing on this site is private.
